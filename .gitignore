.next
node_modules
.repo-git
.env
.env.local
.env.local.*
coverage
test-results
playwright-report
npm-debug.log*
tsconfig.tsbuildinfo
.vercel
.env*

# Sentry Config File
.env.sentry-build-plugin

# Playwright writes these on every run and the cards dealt are random, so the
# bytes differ each time. They are regenerable build output, not layout specs --
# tracking them meant `git status` was never clean after a test run.
artifacts/screenshots/
